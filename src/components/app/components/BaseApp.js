import React from 'react'
import { Navigation } from '../../navigation';
import { connect } from 'react-redux'

const BaseApp = () => {
  return (
    null
  ) 
}

const _mapStateToProps = (state) => {
  return {}
}

export default connect(_mapStateToProps)(BaseApp)
